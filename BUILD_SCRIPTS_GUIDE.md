# 🛠️ دليل سكريبتات البناء المحلي

<div dir="rtl">

## نظرة عامة

توفر هذه السكريبتات طريقة **سهلة وتلقائية بالكامل** لبناء مشروع bara ESP32 محلياً على جهازك. لا تحتاج لأي معرفة تقنية - فقط شغّل السكريبت!

## 📁 السكريبتات المتوفرة

| السكريبت | النظام | الوظيفة |
|---------|--------|----------|
| `build.sh` | Linux/Mac | بناء المشروع وإنشاء ملفات BIN |
| `build.bat` | Windows | بناء المشروع وإنشاء ملفات BIN |
| `flash.sh` | Linux/Mac | فلاش ملفات BIN على ESP32 |
| `flash.bat` | Windows | فلاش ملفات BIN على ESP32 |

## 🚀 الاستخدام السريع

### 1️⃣ البناء

#### Linux/Mac:
```bash
./build.sh
```

#### Windows:
```cmd
build.bat
```

### 2️⃣ الفلاش

#### Linux/Mac:
```bash
./flash.sh /dev/ttyUSB0
```

#### Windows:
```cmd
flash.bat COM3
```

**ملاحظة:** استبدل `/dev/ttyUSB0` أو `COM3` بالمنفذ الصحيح لجهازك.

## 📋 كيف يعمل سكريبت البناء؟

سكريبتات البناء (`build.sh` و `build.bat`) تقوم تلقائياً بـ:

### الخطوة 1: التحقق من Python
يتحقق السكريبت من وجود Python3 مثبت على جهازك.

**إذا لم يكن مثبتاً:**
- **Linux/Mac:** قم بتثبيت Python3 عبر package manager:
  ```bash
  # Ubuntu/Debian
  sudo apt install python3 python3-pip
  
  # macOS (Homebrew)
  brew install python3
  ```

- **Windows:** حمّل من [python.org](https://www.python.org/downloads/)
  - ✅ تأكد من تفعيل "Add Python to PATH" أثناء التثبيت

### الخطوة 2: تثبيت PlatformIO
يقوم السكريبت **تلقائياً** بتثبيت PlatformIO - أداة البناء الاحترافية لـ ESP32.

لن تحتاج لفعل أي شيء!

### الخطوة 3: بناء المشروع
يبني السكريبت مشروع ESP32 بالكامل ويولد ملفات BIN.

### الخطوة 4: تنظيم الملفات
ينسخ السكريبت جميع ملفات BIN إلى مجلد `firmware/`:
- `bootloader.bin`
- `partitions.bin`
- `firmware.bin`
- `INFO.txt` (يحتوي على تعليمات)

### الخطوة 5: إنشاء INFO.txt
يُنشئ ملف `INFO.txt` يحتوي على:
- تاريخ ووقت البناء
- معلومات المنصة
- عناوين ملفات BIN
- تعليمات الفلاش

## 🔥 كيف يعمل سكريبت الفلاش؟

سكريبتات الفلاش (`flash.sh` و `flash.bat`) تقوم بـ:

### 1. التحقق من الملفات
يتحقق من وجود جميع ملفات BIN في مجلد `firmware/`.

**إذا لم توجد:** سيطلب منك تشغيل سكريبت البناء أولاً.

### 2. تثبيت esptool (إذا لزم الأمر)
يثبت `esptool` تلقائياً - أداة فلاش ESP32 الرسمية.

### 3. فلاش الملفات
يقوم بفلاش الملفات الثلاثة على ESP32:
```
0x1000  → bootloader.bin
0x8000  → partitions.bin
0x10000 → firmware.bin
```

### 4. إعادة تشغيل ESP32
يُعيد تشغيل ESP32 تلقائياً بعد الفلاش.

## 🛠️ إيجاد المنفذ الصحيح

### Linux/Mac:
```bash
# قائمة المنافذ المتاحة
ls /dev/tty{USB,ACM}*

# غالباً يكون:
# - /dev/ttyUSB0
# - /dev/ttyACM0
# - /dev/cu.usbserial-* (macOS)
```

### Windows:
```cmd
# افتح Device Manager
# ابحث عن "Ports (COM & LPT)"
# ستجد شيء مثل: "Silicon Labs CP210x USB to UART Bridge (COM3)"

# أو استخدم PowerShell:
Get-WMIObject Win32_SerialPort | Select-Object Name, DeviceID
```

## ✅ التحقق من نجاح البناء

بعد تشغيل سكريبت البناء، يجب أن ترى:

```
╔════════════════════════════════════════════════════════╗
║           ✓ اكتمل البناء بنجاح!                      ║
╚════════════════════════════════════════════════════════╝

📁 الملفات الناتجة:
   → firmware/bootloader.bin
   → firmware/partitions.bin
   → firmware/firmware.bin
   → firmware/INFO.txt

🔥 لفلاش ESP32:
   → ./flash.sh /dev/ttyUSB0
```

تحقق من وجود مجلد `firmware/` يحتوي على الملفات الأربعة.

## ✅ التحقق من نجاح الفلاش

بعد تشغيل سكريبت الفلاش، يجب أن ترى:

```
╔════════════════════════════════════════════════════════╗
║           ✓ اكتمل الفلاش بنجاح!                      ║
╚════════════════════════════════════════════════════════╝

🎉 ESP32 جاهز الآن!

📱 خطوات التشغيل:
   1. أعد تشغيل ESP32
   2. افتح Serial Monitor على: 115200 baud
   3. اتصل بشبكة WiFi: bara
   4. افتح المتصفح: 192.168.4.1
```

## ❌ حل المشاكل الشائعة

### مشكلة: "Python not found"

**الحل:**
- تأكد من تثبيت Python3
- **Windows:** تأكد من إضافة Python إلى PATH
- جرب تشغيل `python --version` أو `python3 --version`

### مشكلة: "Permission denied" (Linux/Mac)

**الحل:**
```bash
# اجعل السكريبت قابل للتنفيذ
chmod +x build.sh flash.sh

# إذا كنت تريد الوصول لـ USB بدون sudo:
sudo usermod -a -G dialout $USER
# ثم أعد تسجيل الدخول
```

### مشكلة: "Failed to connect to ESP32"

**الأسباب المحتملة:**
1. **المنفذ خاطئ:** تحقق من المنفذ الصحيح (راجع "إيجاد المنفذ")
2. **ESP32 غير متصل:** تحقق من كابل USB
3. **التعريفات (Drivers):** قد تحتاج لتعريفات CH340 أو CP210x
4. **Serial Monitor مفتوح:** أغلق أي برامج تستخدم المنفذ
5. **ESP32 في وضع غير صحيح:** اضغط زر BOOT أثناء الفلاش

**الحل المقترح:**
```bash
# جرب منفذ آخر
./flash.sh /dev/ttyUSB1

# أو جرب سرعة أقل
esptool.py --port /dev/ttyUSB0 --baud 115200 ...
```

### مشكلة: "Build failed" (فشل البناء)

**الأسباب المحتملة:**
1. **اتصال إنترنت ضعيف:** PlatformIO يحتاج تحميل مكتبات
2. **مساحة قرص غير كافية:** تحتاج ~1GB
3. **ملفات المشروع تالفة:** حاول `git pull` مرة أخرى

**الحل:**
```bash
# امسح cache PlatformIO
rm -rf .pio/

# أعد تشغيل البناء
./build.sh
```

### مشكلة: "Module not found" (Windows)

**الحل:**
```cmd
REM تأكد من إضافة Python Scripts إلى PATH
REM أو استخدم:
python -m pip install platformio
python -m platformio run
```

## 📊 متطلبات النظام

### الحد الأدنى:
- **CPU:** أي معالج حديث
- **RAM:** 2GB
- **مساحة القرص:** 1GB للـ PlatformIO والمكتبات
- **اتصال الإنترنت:** مطلوب للتحميل الأولي

### البرامج:
- Python 3.7 أو أحدث
- Git (للاستنساخ)
- كابل USB لتوصيل ESP32

## 🎯 أمثلة الاستخدام

### مثال 1: بناء من الصفر (Linux)
```bash
# 1. استنسخ المشروع
git clone https://github.com/ELSHAB7UAHED/bara-esp32-project.git
cd bara-esp32-project

# 2. ابن المشروع
./build.sh

# 3. افلش ESP32
./flash.sh /dev/ttyUSB0

# 4. افتح Serial Monitor لرؤية الـ output
screen /dev/ttyUSB0 115200
```

### مثال 2: بناء من الصفر (Windows)
```cmd
REM 1. استنسخ المشروع
git clone https://github.com/ELSHAB7UAHED/bara-esp32-project.git
cd bara-esp32-project

REM 2. ابن المشروع
build.bat

REM 3. افلش ESP32
flash.bat COM3

REM 4. افتح PuTTY على COM3, 115200 baud لرؤية الـ output
```

### مثال 3: إعادة البناء بعد تعديل الكود
```bash
# 1. عدّل src/main.cpp كما تريد

# 2. أعد البناء
./build.sh

# 3. افلش النسخة الجديدة
./flash.sh /dev/ttyUSB0
```

## 📖 مراجع إضافية

- **[README.md](README.md)** - نظرة عامة على المشروع
- **[INSTALLATION.md](INSTALLATION.md)** - دليل تثبيت شامل
- **[BIN_GUIDE.md](BIN_GUIDE.md)** - دليل ملفات BIN المفصل
- **[PlatformIO Docs](https://docs.platformio.org/)** - توثيق PlatformIO
- **[esptool Docs](https://docs.espressif.com/projects/esptool/)** - توثيق esptool

## 💡 نصائح احترافية

1. **البناء الأول يأخذ وقت:** PlatformIO يحمّل المكتبات (~5-10 دقائق)
2. **البناءات التالية أسرع:** cache يُسرّع العملية (~30 ثانية)
3. **احفظ ملفات BIN:** يمكنك فلاشها لاحقاً بدون إعادة البناء
4. **استخدم Serial Monitor:** لمراقبة ESP32 وحل المشاكل
5. **اقرأ firmware/INFO.txt:** يحتوي على تعليمات مفصلة

## 🆘 الحصول على المساعدة

إذا واجهت مشاكل:

1. **راجع firmware/INFO.txt** - قد تجد الحل
2. **راجع هذا الدليل** - حل المشاكل الشائعة أعلاه
3. **ابحث في Issues** - [GitHub Issues](https://github.com/ELSHAB7UAHED/bara-esp32-project/issues)
4. **افتح Issue جديد** - صف المشكلة بالتفصيل

## ⚖️ الترخيص

هذه السكريبتات جزء من مشروع bara وهي مرخصة تحت MIT License.

---

**ملاحظة:** هذه السكريبتات مصممة لتكون بسيطة وسهلة الاستخدام. إذا كنت مطور متقدم، يمكنك استخدام PlatformIO مباشرة بدون السكريبتات.

</div>
