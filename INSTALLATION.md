# تعليمات التثبيت والاستخدام التفصيلية

## المتطلبات الأساسية

### 1. الأجهزة المطلوبة
- لوحة ESP32 (أي نوع)
- كابل USB من نوع Micro-USB أو USB-C (حسب نوع اللوحة)
- حاسوب (Windows/Linux/Mac)

### 2. البرامج المطلوبة

#### الطريقة الأولى: PlatformIO (موصى بها للمطورين)

1. **تثبيت VS Code:**
   - حمّل VS Code من [هنا](https://code.visualstudio.com/)
   
2. **تثبيت PlatformIO:**
   - افتح VS Code
   - اذهب إلى Extensions (Ctrl+Shift+X)
   - ابحث عن "PlatformIO IDE"
   - اضغط Install

#### الطريقة الثانية: Arduino IDE (أسهل للمبتدئين)

1. **تثبيت Arduino IDE:**
   - حمّل من [هنا](https://www.arduino.cc/en/software)
   
2. **إضافة دعم ESP32:**
   - افتح Arduino IDE
   - اذهب إلى: File > Preferences
   - في "Additional Board Manager URLs" أضف:
     ```
     https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
     ```
   - اذهب إلى: Tools > Board > Boards Manager
   - ابحث عن "esp32"
   - اضغط Install على "esp32 by Espressif Systems"

3. **تثبيت المكتبات:**
   - اذهب إلى: Sketch > Include Library > Manage Libraries
   - ابحث وثبّت:
     - `AsyncTCP` by me-no-dev
     - `ESPAsyncWebServer` by me-no-dev

## طرق التثبيت

### الطريقة 1: رفع الكود من المصدر

#### باستخدام PlatformIO:
```bash
# استنسخ المشروع
git clone https://github.com/ELSHAB7UAHED/bara-esp32-project.git
cd bara-esp32-project

# افتح المشروع في VS Code
code .

# ثم من Terminal في VS Code:
pio run --target upload
```

#### باستخدام Arduino IDE:
1. حمّل المشروع (Download ZIP من GitHub)
2. فك الضغط
3. افتح ملف `src/main.cpp` في Arduino IDE
4. غيّر امتداد الملف إلى `.ino` إذا طلب منك ذلك
5. اختر اللوحة: Tools > Board > ESP32 Dev Module
6. اختر المنفذ: Tools > Port > (اختر المنفذ المناسب)
7. اضغط Upload (السهم →)

### الطريقة 2: استخدام الملفات الجاهزة (bin files)

هذه الطريقة لا تحتاج إلى أي برمجة!

1. **حمّل الملفات الجاهزة:**
   - اذهب إلى [صفحة Releases](https://github.com/ELSHAB7UAHED/bara-esp32-project/releases)
   - أو [صفحة Actions](https://github.com/ELSHAB7UAHED/bara-esp32-project/actions) واختر آخر Build ناجح
   - حمّل ملف `esp32-firmware.zip`
   - فك الضغط

2. **تثبيت esptool:**
   ```bash
   pip install esptool
   ```

3. **رفع الملفات على ESP32:**

   **Linux/Mac:**
   ```bash
   chmod +x flash.sh
   ./flash.sh /dev/ttyUSB0
   ```
   
   **Windows:**
   ```cmd
   flash.bat COM3
   ```
   
   **أو يدوياً:**
   ```bash
   esptool.py --chip esp32 --port /dev/ttyUSB0 --baud 921600 \
     --before default_reset --after hard_reset write_flash -z \
     --flash_mode dio --flash_freq 40m --flash_size detect \
     0x1000 bootloader.bin \
     0x8000 partitions.bin \
     0x10000 firmware.bin
   ```

## معرفة المنفذ (Port)

### Windows:
- افتح Device Manager
- ابحث عن "Ports (COM & LPT)"
- ستجد شيء مثل "Silicon Labs CP210x USB to UART Bridge (COM3)"
- استخدم COM3 (أو الرقم الظاهر)

### Linux:
```bash
ls /dev/ttyUSB*
# أو
ls /dev/ttyACM*
```
عادة يكون `/dev/ttyUSB0`

### Mac:
```bash
ls /dev/cu.*
```
عادة يكون `/dev/cu.usbserial-*`

## الاستخدام بعد التثبيت

1. **افصل ESP32 من الحاسوب وأعد توصيله** (أو اضغط زر Reset)

2. **افتح Serial Monitor** لرؤية المعلومات:
   - Baud Rate: 115200
   - ستظهر لك معلومات مثل:
     ```
     Access Point started:
     SSID: bara
     IP: 192.168.4.1
     Web server started.
     ```

3. **اتصل بشبكة WiFi:**
   - من هاتفك أو حاسوبك
   - ابحث عن شبكة WiFi اسمها: `bara`
   - كلمة المرور: `A7med@Elshab7`

4. **افتح واجهة الويب:**
   - افتح المتصفح
   - اذهب إلى: `http://192.168.4.1`

5. **ابدأ الفحص:**
   - اضغط "SCAN NETWORKS"
   - سترى قائمة بجميع الشبكات المحيطة
   - يمكنك تنفيذ Deauth على أي شبكة

## تخصيص المشروع

### تغيير اسم وكلمة مرور الـ Access Point:

افتح ملف `src/main.cpp` وعدّل السطور:

```cpp
const char* ssidAP = "bara";           // غيّر الاسم هنا
const char* passwordAP = "A7med@Elshab7";  // غيّر كلمة المرور هنا
```

### تغيير عدد حزم Deauth الافتراضي:

```cpp
uint16_t deauthCount = 100; // غيّر الرقم هنا
```

## حل المشاكل الشائعة

### المشكلة: لا يتم اكتشاف ESP32
**الحل:**
- تأكد من توصيل الكابل بشكل صحيح
- جرب كابل USB آخر (بعض الكوابل للشحن فقط)
- ثبّت driver CH340 أو CP2102:
  - Windows: [CH340 Driver](http://www.wch.cn/downloads/CH341SER_EXE.html)
  - Mac: [Driver Guide](https://learn.sparkfun.com/tutorials/how-to-install-ch340-drivers/all)

### المشكلة: فشل الرفع (Upload Failed)
**الحل:**
- اضغط مع الاستمرار على زر BOOT في ESP32 أثناء بدء الرفع
- جرب تقليل Baud Rate إلى 115200
- تأكد من اختيار المنفذ الصحيح

### المشكلة: لا أستطيع الاتصال بشبكة bara
**الحل:**
- تأكد أن ESP32 يعمل (LED يضيء)
- افتح Serial Monitor وتأكد من رسالة "Access Point started"
- جرب إعادة تشغيل ESP32 (زر Reset أو افصل/وصّل الطاقة)
- بعض الهواتف لا تتصل بشبكات بدون إنترنت - عطّل "Auto Switch" للشبكة

### المشكلة: لا يفتح الموقع على 192.168.4.1
**الحل:**
- تأكد أنك متصل بشبكة `bara`
- جرب `http://192.168.4.1` (مع http:// وليس https://)
- جرب من متصفح آخر
- عطّل بيانات الهاتف المحمول إذا كنت تستخدم الهاتف

## ملاحظات مهمة

⚠️ **تحذير قانوني:**
- استخدم هذه الأداة فقط على شبكاتك الخاصة
- الاستخدام غير المصرح به قد يكون جريمة في بلدك
- المطور غير مسؤول عن سوء الاستخدام

📚 **للتعلم:**
- هذا المشروع تعليمي لفهم كيفية عمل WiFi وبروتوكول 802.11
- مفيد لاختبار أمان شبكتك الخاصة

🔧 **للمطورين:**
- يمكنك تعديل الكود حسب احتياجاتك
- المشروع مفتوح المصدر - ساهم في تطويره!

## الدعم والمساعدة

إذا واجهت أي مشكلة:
1. تحقق من قسم [Issues](https://github.com/ELSHAB7UAHED/bara-esp32-project/issues)
2. افتح Issue جديد مع تفاصيل المشكلة
3. شارك أي رسائل خطأ أو صور للمشكلة

---

**المطور:** Ahmed Nour Ahmed من قنا، مصر 🇪🇬

**المستودع:** https://github.com/ELSHAB7UAHED/bara-esp32-project
